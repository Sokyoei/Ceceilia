project(
    'Ceceilia',
    'cpp', 'c', 'cuda',
    version: '0.0.1'
)

cxx = meson.get_compiler('cpp')
cc = meson.get_compiler('c')

config_h = include_directories('.')

subdir('cc')
# subdir('cuda')
